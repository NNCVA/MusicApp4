# AGENTS.md

## 重要
- 必须使用中文输出！！！
- 注意，必要时使用 .agents/skills/ 目录下的skills ！！！
- 项目文档全放在 docs/ 目录下！！！
- 多使用子代理并行执行
- 调用所有工具（Tool）时，必须严格检查并确保包含 schema 中规定的所有必填字段（例如调用 `task` 工具必须传入 `description`、`prompt`、`subagent_type`），彻底杜绝由于缺失必填参数而引发的 SchemaError 工具失败。

## 项目概述
MusicApp 是一款功能丰富的现代化 Android 本地音乐播放器应用，基于 Kotlin 与 Jetpack Compose 构建。
它支持 7 种主流音频格式、本地目录扫描、完整的 Media3/ExoPlayer 引擎集成（结合 MediaSession 及通知栏控制）、同步歌词（内嵌歌词与外部 `.lrc` 文件）、多主题与 Material You 动态取色、Aero 动态 Canvas 特效背景、中英双语本地化、播放列表管理、播放历史记录，以及平滑的淡入淡出切歌过渡。

## 核心功能与需求汇总（26 核心需求）
1. **音频文件扫描器**：支持 7 种音频格式（.mp3, .flac, .wav, .aac, .m4a, .ogg, .opus）的设备音频文件扫描，支持自定义路径过滤，扫描时伴有雷达脉冲波动画。
2. **歌曲列表**：展示所有扫描到的音频，支持多选、批量操作，以及按标题/艺术家/专辑/添加时间/时长排序。
3. **侧边栏路由导航**：Drawer/Rail 抽屉式侧边栏导航，快速进入应用各个核心板块（单曲、专辑、艺术家、播放列表、播放历史、文件夹浏览、设置、关于）。
4. **播放控制**：基础播放动作（播放、暂停、上一首、下一首、快进/快退、进度条拖拽控制）。
5. **播放模式**：顺序播放/列表循环、随机播放、单曲循环。
6. **淡入淡出切歌**：切歌时通过音量平滑过渡（Crossfade），消除音频硬切造成的爆音与喀嗒声。
7. **播放列表管理**：创建、重命名、删除播放列表，批量添加/移除歌曲。
8. **播放列表详情**：查看特定播放列表内的歌曲，并支持一键播放全列表。
9. **播放历史记录**：自动记录并展示用户最近播放过的歌曲历史。
10. **同步歌词**：支持外部 `.lrc` 文件与内嵌歌词（SYLT/USLT），实时高亮显示当前演唱行并平滑自动滚动居中；若无时间戳歌词，自动回退至静态平滑展示。
11. **专辑封面提取**：自动从音频文件元数据中提取内嵌专辑封面，并提供缺省占位图与视觉效果。
12. **迷你播放器 (Mini Player)**：屏幕底部常驻展示当前播放歌曲信息及快捷控制按钮。
13. **全屏播放器 (Full Screen Player)**：可向上滑动展开，包含 3 个可水平滑动的页面（封面大图页、同步歌词页、当前播放队列页）。
14. **播放队列**：实时查看当前播放队列，支持拖拽重排、移除歌曲以及点击快速跳转播放。
15. **曲目/专辑/艺术家/文件夹分类浏览**：按不同维度的分类浏览本地音乐库。
16. **通知栏控制**：前台服务通知栏集成，后台状态下可通过通知栏控制播放、暂停、切歌与查看歌曲信息。
17. **MediaSession 系统集成**：与 Android 系统底层 MediaSession 深度绑定，支持锁屏控制、蓝牙耳机按键控制及 Audio Focus 音频焦点控制（来电自动暂停等）。
18. **歌曲信息查看器**：通过底部弹窗（Bottom Sheet）展示歌曲详细技术元数据（编码格式、比特率、采样率、文件大小、物理路径等）。
19. **运行时权限管理**：精细化适配不同 Android 版本的权限（Android 13+ 的 `READ_MEDIA_AUDIO` / `POST_NOTIFICATIONS` 及低版本的 `READ_EXTERNAL_STORAGE`）。
20. **多主题切换**：支持 Android 12+ Material You 壁纸动态取色，以及预置的多套精选主题。
21. **浅色 / 深色模式**：每套主题均完整支持 Light / Dark 模式。
22. **Aero 动态背景**：动态 Canvas 背景视觉特效（流体网格 Fluid Mesh、光晕气场 Glow Aura、纯色静态），支持低电量自动暂停降级保护。
23. **中英双语切换**：界面全字符串本地化（English / 简体中文），支持跟随系统或手动指定。
24. **扫描器雷达动画**：在执行文件夹扫描时提供扩散脉冲视觉反馈。
25. **设置与偏好存储**：应用配置管理（主题选择、语言切换、Aero 背景模式、淡入淡出时长、扫描路径过滤、重置应用配置等）。
26. **关于页面**：展示应用版本号、开发者元数据、开源许可协议及致谢清单。

## 技术栈与架构设计
- **开发语言**：Kotlin 2.x
- **UI 框架**：Jetpack Compose, Material 3
- **导航框架**：Jetpack Navigation 3 (`androidx.navigation3`)
- **媒体播放引擎**：AndroidX Media3 (ExoPlayer + MediaSession + MediaLibraryService)
- **状态与架构模式**：单向数据流 (UDF), ViewModel, Kotlin Coroutines, StateFlow
- **数据持久化**：Room Database (音频与播放列表缓存) & DataStore Preferences (用户偏好设置)

## 编码规范与约定
- 保持严格的响应式数据流，使用 StateFlow/SharedFlow 暴露 ViewModel 状态。
- 解耦 UI Composable 与系统底层服务 —— 始终通过 ViewModel 和 Repository 进行交互。
- 严禁硬编码文本，所有界面字符串必须定义在资源文件中 (`values/strings.xml`, `values-zh-rCN/strings.xml`)。
- 遵循 Android Edge-to-Edge 边到边沉浸式设计规范与 Material 3 设计指南。

## 架构与设计参考文档 (设计规范提示词)
- 目前暂无项目文件
