# Wave 6：分类浏览、播放列表与历史

状态：`TODO`

## 目标与进入条件

交付分类、文件夹、播放列表、历史和批量操作的真实数据闭环。进入条件为 Wave 2 共享组件、Wave 3 媒体库查询和 Wave 4 播放建队契约 `DONE`；与 Wave 5 并行时不得同时修改共享组件或 MediaController 协议。

## 执行单元概览

| ID | 状态 | 交付结果 | 依赖 | 最窄验证 |
|---|---|---|---|---|
| W6-01 | `TODO` | 专辑/艺术家查询与详情 | Wave 3 | DAO + Compose 测试 |
| W6-02 | `TODO` | 文件夹树与递归播放 | Wave 3、Wave 4 | 纯逻辑 + Compose 测试 |
| W6-03 | `TODO` | 播放列表事务用例 | Wave 1、Wave 4 | Room 设备测试 |
| W6-04 | `TODO` | 播放列表页面与播放全部 | W6-03 | Compose 测试 |
| W6-05 | `TODO` | 播放历史页面与清空 | Wave 4 | DAO + Compose 测试 |
| W6-06 | `TODO` | 曲目多选与批量动作 | W6-03、Wave 4 | Reducer + Compose 测试 |
| W6-07 | `TODO` | 排序/状态、截图与播放列表旅程 | W6-01～W6-06 | 截图 + Android journey |

## W6-01：专辑与艺术家

状态：`TODO`

- 目标：按稳定 Album ID、Artist ID 派生集合、详情和“播放全部”上下文。
- 边界：分类 DAO/Repository、专辑/艺术家根页与详情页；不拆分合作艺术家标签。
- 验证：同名不同身份、不可用/隐藏曲目、排序与空态 DAO 测试；根页/详情 Compose 测试。
- 完成：网格和详情均来自 Room，播放全部按当前排序构建上下文。
- 执行记录：开始—；完成—；执行者—；提交—；证据—；阻塞—。

## W6-02：文件夹树

状态：`TODO`

- 目标：按存储卷构建真实目录树，并递归收集后代可用曲目播放。
- 边界：目录树纯模型/构建器、Repository 查询和文件夹页面。
- 验证：根目录、空目录、同名跨卷、深层后代、失效曲目和确定排序的本地测试；页面 Compose 测试。
- 完成：递归播放不跨卷、不包含隐藏/不可用曲目，空结果不替换现有队列。
- 执行记录：开始—；完成—；执行者—；提交—；证据—；阻塞—。

## W6-03：播放列表事务用例

状态：`TODO`

- 目标：实现创建、重命名、删除、批量加入/移除和“播放全部”数据用例。
- 边界：PlaylistRepository 实现与用例；不实现页面布局。
- 验证：真实 Room 覆盖唯一名称、位置、重复跳过、新增/跳过计数、原子回滚及失效关联。
- 完成：列表内固定加入顺序且无拖拽排序；空或无可用曲目时不替换队列。
- 执行记录：开始—；完成—；执行者—；提交—；证据—；阻塞—。

## W6-04：播放列表页面

状态：`TODO`

- 目标：交付列表根页、创建/重命名/删除交互、详情、批量移除和一键播放全部。
- 边界：`feature/playlists` ViewModel/Reducer/Compose；只依赖 W6-03 与 MediaController 门面。
- 验证：加载/空/错误、确认框、重复名称、部分不可用反馈和状态恢复 Compose 测试。
- 完成：所有操作有双语反馈，失败不留下部分 UI 或数据库状态。
- 执行记录：开始—；完成—；执行者—；提交—；证据—；阻塞—。

## W6-05：播放历史

状态：`TODO`

- 目标：展示最近播放时间、累计次数并提供确认后的清空历史。
- 边界：HistoryRepository 查询、ViewModel 与页面；历史计时写入仍归 Wave 4。
- 验证：同曲目更新、排序、格式化、空态、清空事务和失败恢复测试。
- 完成：清空只删除历史，不影响媒体库、播放列表或快照。
- 执行记录：开始—；完成—；执行者—；提交—；证据—；阻塞—。

## W6-06：曲目多选与批量动作

状态：`TODO`

- 目标：完成长按进入多选、当前筛选结果全选、加入播放列表/队列、下一首播放和隐藏。
- 边界：共享多选状态仅做兼容扩展，业务动作位于单曲页用例；不得物理删除文件。
- 验证：返回先退出多选、筛选后全选、选择顺序、批量失败、随机模式插入及隐藏后刷新测试。
- 完成：上下文栏动作均经 Repository/MediaController，选择状态在动作完成后可预测地清理。
- 执行记录：开始—；完成—；执行者—；提交—；证据—；阻塞—。

## W6-07：页面状态与 Wave 验收

状态：`TODO`

- 目标：统一各页独立排序、加载/空/错误状态，建立截图并完成播放列表旅程。
- 边界：W6 页面整合、字符串、截图基线和 journey；不修订冻结导航协议。
- 验证：分类/播放列表/历史九尺寸截图；Android journey 覆盖创建、批量加入、播放全部、移除和删除。
- 完成：W6-01～W6-06 `DONE`，核心逻辑行 80%/分支 70%，Debug/Lint/架构检查通过。
- 执行记录：开始—；完成—；执行者—；提交—；证据—；阻塞—。

## Wave 6 门禁

状态：`TODO`

全部媒体业务一级入口使用真实 Room 数据；批量操作保持事务原子性且不物理删除音频；与 Wave 5 的并行改动已完成共享协议合并检查。
