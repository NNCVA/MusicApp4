# 使用单播放器淡出淡入切歌

MusicApp 使用单个 ExoPlayer 将前曲淡出至静音后切换媒体项，再将后曲淡入。该方案优先保证 MediaSession、音频焦点与播放队列只有一个状态来源；暂不采用需要双播放器并行解码、混音及同步状态的重叠式交叉淡化。过渡总时长由用户在 `0–2000 ms` 范围内以 `250 ms` 步进配置，默认 `500 ms`，播放逻辑读取当前设置而不写死实际时长。
