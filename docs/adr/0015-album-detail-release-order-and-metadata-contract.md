# 专辑详情优先使用发行曲序并采用可靠元数据摘要

专辑详情页采用有效“碟号 → 音轨号”的发行顺序，完全缺失曲序时回退到歌曲标题默认升序，部分缺失或冲突项稳定置后；页面不提供排序入口，也不保存详情排序状态。Hero 的年份、编码、比特率、采样率和位深只有在专辑曲目形成可靠单一值时显示，否则显示 `➖`，质量标签则按曲目去重并列展示。该决策保留专辑发行语义与截图的曲序认知，同时避免从不完整标签猜测数据；其他分类详情页的通用排序契约不受影响。
