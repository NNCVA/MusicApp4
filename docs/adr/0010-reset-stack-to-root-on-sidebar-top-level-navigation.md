# 侧边栏一级入口导航一律重置至根页面

MusicApp 在用户通过侧边栏点击任意一级入口（单曲、专辑、艺术家、歌单、播放历史、文件夹、扫描音乐、设置、关于）时，一律将目标返回栈重置（Reset）为单元素根页面栈，直接展示该入口的列表根页。

该方案确立了清晰直接的层级心智模型：侧边栏各入口作为顶级直达导航，不保留跨入口切换前未退出的二级详情页，从根本上避免从其他页面切入时意外呈现历史二级子页面的割裂感；在单个一级栈内部，详情页的返回键操作（Back Navigation）仍保持逐级出栈退回上一级上下文的正常行为。
