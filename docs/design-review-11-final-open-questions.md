# 首版最终未决盘问 11

状态：已接受（2026-07-28）

1. **平台基线**：正式支持 `minSdk 26`、`targetSdk 36`，不为 API 25 及以下提供兼容。
2. **默认播放模式**：首次启动使用随机关闭、循环关闭，并按当前播放上下文顺序播放。
3. **下一首与队尾**：手动“下一首”始终尝试进入下一项；单曲循环只影响自然播放结束，列表循环在队尾回到队首，循环关闭在队尾停止并保留最后曲目。
4. **随机序列**：开启随机时生成并持久化一份稳定随机顺序，当前曲目保持不变；关闭随机后恢复原队列顺序并保留当前曲目。
